# 066.加一
## 解题思路
从数组最后一位开始遍历，因为加一操作一定是从个位开始。如果当前数字不是 9，直接加一后返回即可，因为不会再产生进位；如果当前数字是 9，加一后会变成 0，因此将当前位置赋值为 0，并继续向前处理进位。如果整个数组遍历结束都没有返回，说明原数组每一位都是 9，例如 999，此时需要创建一个长度加一的新数组，将第一位赋值为 1，其余位置默认都是 0，最终得到 1000。