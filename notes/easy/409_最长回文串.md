# 409.最长回文串
## 思考过程
这里用到了之前做力扣第三题学到的用ascll创建一个数组映射来代替哈希表，这样可以减少空间复杂度，之后再遍历字符串，每遇到一个字母就将它的次数加一，当次数等于二的时候让回文串长度加二，然后让字符出现次数重新记为0，因为回文串的构成需要左右两个相同字符，所以这样就可以记录到所可以放在左右的字符总个数，而回文串是可以在最中间的位置放一个无重复字符的，所以最后再判断字符串里是否还有多的字符放在中间，只要判断现有的回文串长度是否跟字符串长度相等就好了。有的话就再让长度加一