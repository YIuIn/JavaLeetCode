# 088.合并两个有序数组
## 解题思路
由于 nums1 的后半部分预留了空间，如果从前往后合并，会覆盖掉还没有比较的元素，因此采用从后往前合并。定义三个指针：i 指向 nums2 的最后一个有效元素，j 指向 nums1 的最后一个有效元素，k 指向 nums1 的最后一个位置。每次比较 nums1[j] 和 nums2[i]，将较大的元素放到 nums1[k]，然后移动对应指针。当 nums1 的有效元素全部放完后，只需要将 nums2 剩余的元素依次复制到 nums1 中即可；如果 nums2 先放完，则说明 nums1 剩余元素本身就在正确位置，无需再处理