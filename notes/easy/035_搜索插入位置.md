# 035.搜索插入位置
## 解题思路
使用二分查找解决。定义左右指针 i 和 j，再定义一个 ans 用来记录答案，初始值设为 nums.length，因为如果目标值比数组中所有元素都大，插入位置就是数组末尾。每次取中间位置 mid，如果 target <= nums[mid]，说明答案可能就是 mid，也可能在它左边，所以先将 ans 更新为 mid，再继续向左查找；否则说明目标值只能在右半部分，将左指针移动到 mid + 1。循环结束后，ans 中保存的就是第一个大于等于 target 的位置，也就是目标值存在时的位置，或者目标值不存在时应该插入的位置。