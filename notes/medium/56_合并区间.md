# 56.合并区间
## 思考过程
找可以合并的区间，首先是要判断两个区间是否有重合的地方，即一个区间的头是否被另一个区间包住了，然后再去对比尾的位置，但是如果是每个区间都比对一遍就太麻烦了，所以直接按照数组头排序，这样可以保证每个数组的头只会越来越大，那我只要先把遍历到的数组保存下来，然后向后遍历的同时判断后面的数组头是否小于保存下来的数组尾，小于就再将数组尾更新为更大的那个，否则就把保存的数组先添加到要返回的集合中再将此数组保存下来就好了。