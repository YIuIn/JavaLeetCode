# 560.和为K的子数组
## 思考过程
这道题我一开始上来想到的是滑动窗口的方法，然后发现不对，如果遇到负数的情况的话就不行了，所以滑动窗口的方法只能用在移动时有单调性的数组里面啊。正确是思路应该是用前缀和的思想，就是说定义一个sum用来存储现在遍历到的值的前缀和，然后定义一个hashmap来存储前缀和跟它出现的次数，因为有负数的情况下同一个前缀和可以出现多次，每遍历到一个数的时候拿现在的sum减去目标值K为键去map集合里面找出现的次数，即找到现在为止的数组总和减去从头到某个位置的和等于这个目标值，看这钟情况出现几次，就代表有几个子数组可以等于目标值，就让计数器加几。