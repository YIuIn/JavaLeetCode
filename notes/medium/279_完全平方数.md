# 279.完全平方数
## 思考过程
因为是求目标值是多少个平方数的和的最小值，很容易想到动态规划，想不到去做动态规划的简单题理解这个思路，这题其实做法很简单，就是不断更新迭代取最小值就好了，直接从1开始遍历，没到一个数就求它需要的最小平方数和，然后一路迭代，至于求最小的方法，先定义一个无穷大的值，其实定义为外层循环的i也可以，因为1肯定能组成这个数，之后从一开始，求i减去j的平方得到的下标对应的次数，因为一路迭代的原因，所以这个数可以保证是到这个次数的最小值，再加上j的平方可以到达目标值说明只需要这个最小值加1就能得到到i需要的平方数了然后一路遍历取最小值就好了