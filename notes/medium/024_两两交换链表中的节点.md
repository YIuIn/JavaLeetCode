# 24.两两交换链表中的节点
## 思考过程
想要依次交换链表中的节点，首先得先判断接下来是否还有节点可以交换，而交换要改变三个指向，对于n节点跟n.next节点首先要让n的上一节点指向n的下一节点，n指向n的下下节点，然后n的下一节点指向n所以我们定义一个虚拟头节点，从此处开始，虚幻结束的条件是没有两个节点可以交换，之后再依次操作就好了，每次交换后面的两个节点，然后把头指针指向后面的第二个节点。之后还有一种递归的方式，其实对于链表操作经常能看到递归的方式，为的就是可以倒序操作链表，将因为这样就可以一次只改变两个节点的指向，后续会操作前面的节点。