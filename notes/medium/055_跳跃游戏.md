# 55.跳跃游戏
## 思考过程
这题官方题解是正着跳的，主要是更新一个能跳的的最远距离，但是这样每次要判断遍历到的地方是否可以跳过来，还要一直更新跳的最远的距离，判断最远距离是否已经到达数组长度，很繁琐，而如果转变一下思路，倒序遍历，则只需要做一次判断就好了，从倒数第二个节点开始，更新一个需要到达的最大距离，如果一个位置能到达最远距离，那么说明这个位置就能跳到最后一个节点，也就是说这个节点能跳到最后一个位置了，那我只需要跳到这个位置就能跳到最后一个位置，每次更新维护一个需要到达的最远距离就好，如果最后这个值为0了，代表可以从0跳到数组末尾，此时返回true，否则返回false