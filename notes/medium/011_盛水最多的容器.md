# 11.盛水最多的容器
## 思考过程
要求最大的面积首先要想到面积怎么求，首先要一个长跟一个宽，长由更短的柱子决定，宽则是由柱子间的距离决定，而想遍历整个数组的容器，自然是用头尾指针，因为面积是由短边决定的，所以每次让较短的边移动，面积才会变大，官方解法是每次移动都算一次面积然后比较历史最大值来更新，只需要加一个左右最高值，让移动后这个值更新时再做计算判断是否更新历史面积最大值，就可以减少很多操作。