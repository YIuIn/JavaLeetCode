# 19.删除链表的倒数第n个节点
## 思考过程
这题的思想还是比较简单的，就是找到那个要删除节点的位置，然后再将它的上一个节点指向他的下一个节点就好了，但是我这里讲一种递归的思想，创建一个递归方法，把节点跟目标值n传递进去，如果下一个节点不为空就一直调用方法本身遍历到下一个节点，一直到最后一个节点是返回1，别的都返回上一个返回值+1，然后再传递返回值时做一个判断，如果返回值跟n相等就删除这个节点。